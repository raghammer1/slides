export let containerWidth = 1000;

export const setContainerWidth = (newWidth) => {
  containerWidth = newWidth;
};

export let containerHeight = 500;

export const setContainerHeight = (newWidth) => {
  containerHeight = newWidth;
};
