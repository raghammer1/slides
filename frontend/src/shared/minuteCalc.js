export const minutesCalculator = (milliseconds) => {
  const minutes = milliseconds / 60000;
  return minutes;
};

// const secondObject = elements[1]; // Access the second object
// const timeKey = Object.keys(secondObject)[0]; // Get the first key of the object
// const elementsAtThisTime = secondObject[timeKey]; // Access the value using the key

// console.log("Time:", timeKey); // Output the time
// console.log("Elements at this time:", elementsAtThisTime); // Output the elements at this time
